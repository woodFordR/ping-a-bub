# testing
ENVIRONMENT=testing
TESTING=1

# database
DB_HOST=localhost
DB_NAME=pingabub_test
DB_USER=pingabub
DB_PORT=5432
DB_PWORD=pingabub
DB_URL="postgresql://${DB_USER}:${DB_PWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

